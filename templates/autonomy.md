# Autonomy
This replaces asking before a push or a PR. On hobby and non-client-facing projects, deliver every change as a PR: commit, push, open it, report the link. The `manager` runs on the autonomous dial: only the hard lines and a genuine requirements or preference fork reach the human.
