# <Backlog title>

## [pending] [P1] Replace with task title

**Context** (captured YYYY-MM-DD): what we were doing when this came up, who asked, why it
matters, the relevant files/decisions/constraints, and what "done" looks like.

```
Replace this with a self-contained, ready-to-run prompt authored now from the live context.
It must stand alone — name the files/paths, repeat the decisions and constraints, state the
goal — so a fresh agent with no memory of this session can run it verbatim. Multi-line is fine.
```

Log:
- created YYYY-MM-DD

---

## [pending] [P3] Replace with task title

**Context** (captured YYYY-MM-DD): why this is wanted and the situation it came from.

```
Self-contained prompt for the second item.
```

Log:
- created YYYY-MM-DD

---
