# <Process> — Run Index

Chronological log of runs. Newest first. Each row = one run cycle.
Process and format are in [RUNBOOK.md](RUNBOOK.md); driven by `/recurring-improvement`.

| Date | New | Closed | Regressed | Headline |
|------|-----|--------|-----------|----------|
| _(no runs yet)_ | — | — | — | Scaffolded; awaiting first run. |

---

## How to read this index

Each run produces a dated folder under `runs/`. `New` = stable IDs opened this
run; `Closed` = IDs moved to `fixed`/`wontfix`; `Regressed` = previously-fixed
IDs that came back. The headline captures the run's most important movement.
